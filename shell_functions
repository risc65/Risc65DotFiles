findii() {
    local cmd="find ."
    for pattern in "$@"; do
        cmd+=" | grep -i -- \"$pattern\""
    done
    eval "$cmd"
}

parse_git_branch() {
    git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/ (\1)/'
}
